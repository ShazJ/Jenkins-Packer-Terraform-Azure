   stage('Clone repository') {
        checkout scm
    }
    try {
        stage('Setup stuff') {
            // Pull latest container
            sh label: Setup stuff"
                script: ls
        }
        stage('Deploy environment - todo') {
        }
